server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LEI-Centenario/microservice
          search-paths: confs
          username: LEI-Centenario
          password: Leiwangli1806!!
      label: master

